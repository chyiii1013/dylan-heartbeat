TARGET_API_URL=https://api地址/v1/chat/completions
# 大部分变量都可以在管理页 /admin 的「设置」tab 里改，保存后写入这个 .env、重启生效。
# 下面这组刻意不放进管理页，需要手改：RESTART_COMMAND、ADMIN_USER、ADMIN_PASSWORD、
# DATA_DIR、DIARY_DIR、PORT、GATEWAY_BASE_URL。
TARGET_API_KEY=你的APIKey
GATEWAY_API_KEY=公网访问网关时使用的长随机Key
MODEL_NAME=你的模型名称
# 思考模式（DeepSeek）：网关聊天默认关思考。RikkaHub 聊天/工具调用会带 tools，
# 思考模式下 DeepSeek 强制要求回传 reasoning_content，RikkaHub 不回传会报 400、消息发不出去。
GATEWAY_CHAT_REASONING=off
# 思考模式（DeepSeek）：唤醒/日记默认开思考。唤醒请求不带 tools、无需回传，
# 开思考能让"我"保有自我连续性、认得出自己写的东西。
WAKE_REASONING=on
# 推送标题与对话里对用户的称呼（可选）。AI_DISPLAY_NAME 留空时标题用「来自姐姐」。
AI_DISPLAY_NAME=
USER_DISPLAY_NAME=
BARK_KEY=你的BarkKey
CUSTOM_ICON_URL=https://你的图标URL（可选）
ALLOW_PUBLIC_API=false
PUSH_PROVIDER=bark
NTFY_SERVER_URL=https://ntfy.sh
NTFY_TOPIC=
NTFY_TOKEN=
NTFY_PRIORITY=
NTFY_TAGS=
DIARY_ENABLED=true
DIARY_DIR=diary
DATA_DIR=
PUSH_TIMEOUT_MS=15000
WAKE_UPSTREAM_TIMEOUT_MS=300000
REQUEST_BODY_LIMIT_MB=50
MULTIMODAL_MODE=passthrough
DAY_WAKE_AFTER_MINUTES=60
NIGHT_WAKE_AFTER_MINUTES=120
DAY_CHECK_INTERVAL_MINUTES=10
NIGHT_CHECK_INTERVAL_MINUTES=120
WAKE_DAY_START_HOUR=10
WAKE_DAY_END_HOUR=24
WEATHER_ENABLED=false
WEATHER_LOCATION_NAME=Beijing
WEATHER_LAT=
WEATHER_LON=
WEATHER_UNITS=metric
PORT=3000
GATEWAY_BASE_URL=http://localhost:3000
TIME_ZONE=Asia/Shanghai
RESTART_COMMAND=pm2 restart gateway wake-up --update-env
ADMIN_USER=admin
ADMIN_PASSWORD=你的强密码
